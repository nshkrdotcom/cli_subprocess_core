# CliSubprocessCore 0.4.0 release preparation

Prepared on 2026-07-27. This repository is intentionally committed and pushed
without publishing to Hex or creating `v0.4.0`.

## Release scope

- total structured-output and completion-only provider manifests;
- typed `ProviderFeatures.Error` rejection;
- generic secure owner-tracked ephemeral files;
- higher-fidelity Amp and Antigravity terminal projection;
- shared dependency-source helper v6 and target SDK release edges;
- Elixir 1.20 warnings-as-errors normalization.

## Publication order

The Execution Plane prerequisites for Core are already published. Publish and
tag Core first, then publish and tag `antigravity_cli_sdk 0.2.0` and
`amp_sdk 0.7.0`, and finally `agent_session_manager 0.12.0`.

Before the release operation, verify the changed repository and generated
build artifacts are owned by the intended `home:home` account. Publication and
tagging are deliberately outside this preparation change.
