# `cli_subprocess_core` 0.2.0 Release Preparation

Date: 2026-07-13

Expected tag after verified Hex publication: `v0.2.0`

## Execution Plane Input

- Source commit: `62d699d1db0d3ad02581b3dcf896db63e8daea5a`
- Generated projection commit: `cd06c3785b439617267a88720adc22a61de07781`
- Projection branch: `projection/execution_plane`
- Projection tree digest:
  `153fed8101bdfb6f856e1159a284057d5232c792edd2aa9fbf6bd01bf5601e45`
- Selected source components: core Execution Plane, JSON-RPC, and process
  runtime, projected into one `execution_plane` OTP application.

## Dependency Evidence

Local development resolves:

```text
cli_subprocess_core
├── execution_plane (../execution_plane/dist/monolith/execution_plane)
│   ├── erlexec ~> 2.3
│   ├── ground_plane_contracts ~> 0.1.0
│   ├── ground_plane_persistence_policy ~> 0.1.0
│   ├── jason ~> 1.4
│   └── telemetry ~> 1.3
├── ground_plane_contracts (local prepared-state override)
├── ground_plane_persistence_policy (local prepared-state override)
├── jason ~> 1.4
└── zoi ~> 0.18
```

The published package dependency surface contains one Execution dependency:

```text
execution_plane ~> 0.1.0
jason ~> 1.4
zoi ~> 0.18
```

The local Ground entries are top-level prepared-state overrides used while the
two parent packages are still unpublished. Hex mode omits them, so they are
not package dependencies.

## Verification Evidence

- Unchanged baseline `mix ci`: green, 304 tests, 0 failures, 4 excluded;
  Credo and Dialyzer green.
- Unchanged baseline `./scripts/model_selection_ci.sh ci`: green for
  `cli_subprocess_core`, `codex_sdk`, `claude_agent_sdk`, `amp_sdk`, and
  `agent_session_manager`.
- Dependency and generated-module focused suite: green, 29 tests, 0 failures.
- Generated Execution module count compiled by the focused suite: 88 files.
- Post-change `mix ci`: green, 312 tests, 0 failures, 4 excluded; Credo and
  Dialyzer green.
- Post-change `./scripts/model_selection_ci.sh ci`: green for all five selected
  repositories. One Codex streaming test produced a single load-sensitive
  failure before passing 21 focused repetitions and the complete rerun.
- Isolated generated-artifact QC with all raw Execution source component paths
  absent: green.
- Root projection clone at `cd06c3785b439617267a88720adc22a61de07781`:
  compiled 88 generated files and passed the focused integration suite.

## Package Evidence

- Package file count: `99`
- Sorted package file-list checksum (SHA-256):
  `3f9a320c1e91a6283ddd3e313cd552d00cfbc75d8b62230a4b00df05746c3998`
- Package tarball checksum (SHA-256): recorded in the central program receipt
  after the final immutable tarball is built.
- Clean tarball consumer: green; recorded in the central program receipt.
- `mix hex.publish --dry-run --yes`: expected to fail only because
  `execution_plane 0.1.0` is not yet published.
